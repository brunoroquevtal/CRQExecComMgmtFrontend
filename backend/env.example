# Porta do servidor
PORT=3000

# Modo debug (true/false)
API_DEBUG=false

# URL base da API (para CORS)
API_BASE_URL=http://localhost:3000

# Supabase Configuration (opcional - se não configurado, usa SQLite)
# SUPABASE_URL=https://seu-projeto.supabase.co
# SUPABASE_ANON_KEY=sua-chave-anon-key
# SUPABASE_SERVICE_ROLE_KEY=sua-chave-service-role-key (opcional, use apenas se precisar de permissões elevadas)

# Domínios permitidos para auto cadastro (separados por vírgula)
ALLOWED_EMAIL_DOMAINS=vtal.com